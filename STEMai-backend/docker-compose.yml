version: "3.9"

services:
  backend:
    build: .
    container_name: stembotix-backend
    ports:
      - "8123:8123"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - APP_ENV=production
    restart: always
